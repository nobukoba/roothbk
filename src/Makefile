CURRDIR = $(PWD)

all:
	cd $(CURRDIR)/minipacklib; $(MAKE)
	cd $(CURRDIR)/roothbklib;  $(MAKE)
	cd $(CURRDIR)/exec;        $(MAKE)

clean:
	cd $(CURRDIR)/minipacklib; $(MAKE) clean
	cd $(CURRDIR)/roothbklib;  $(MAKE) clean
	cd $(CURRDIR)/exec;        $(MAKE) clean

